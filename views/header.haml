.header
  .title
    %h1
      ="Professor Bingo!"
  .login_info
    - if session[:user]
      = "Welcome " + session[:user].email
    - else
      %a{:title => "Register", :id => "Register", :href => "/register/"} Register
      %br
      %a{:title => "Login", :id => "Login", :href => "/login/"} Login
  - if @message
    .message
      =@message
  