%h3
  ="Listing of schools:"
- School.all.each do |school|
  %br
  - if(session[:user].superadmin?)
    %a{:title => "Edit #{school.name}", :href => "/controlpanel/school/edit/#{school.url_safe_name}"} #{school.name}
  - else
    ="#{school.name}"
