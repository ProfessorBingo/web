%h3
  ="Listing of users:"
- Student.all.each do |user|
  %br
  - if((session[:user].superadmin? && user.superadmin?) || (!user.superadmin?))
    %a{:title => "Edit #{user.email}", :href => "/controlpanel/user/edit/#{CGI::escape(user.email)}"} #{user.email}
  - else
    ="#{user.email}"
